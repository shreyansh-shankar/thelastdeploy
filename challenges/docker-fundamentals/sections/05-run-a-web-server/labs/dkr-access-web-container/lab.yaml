id: dkr-access-web-container
title: "Access web container"
xp: 35
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "docker rm -f secret-web || true"
    - "docker run -d --name secret-web -p 8082:80 nginx:alpine"
    - "mkdir -p $HOME/docker-test"
    - "rm -f $HOME/docker-test/web_status.txt"
version: 1
