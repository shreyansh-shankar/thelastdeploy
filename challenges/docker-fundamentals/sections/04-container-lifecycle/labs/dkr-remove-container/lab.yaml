id: dkr-remove-container
title: "Remove a container"
xp: 30
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "docker rm -f old-trash-container || true"
    - "docker create --name old-trash-container alpine echo 'exited'"
version: 1
