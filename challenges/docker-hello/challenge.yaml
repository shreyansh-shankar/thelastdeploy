id: docker-hello
title: "Run your first container"
description: "Run a hello-world Docker container and verify it exits cleanly."
topic: docker
difficulty: beginner
xp: 50
estimated_minutes: 5
resource_limits:
  cpu: 1
  memory_mb: 512
setup:
  type: shell
  seed_commands: []
steps:
  - title: "Run the container"
    body: "Run: docker run hello-world"
    hint: "Make sure Docker Desktop is running first."
validator:
  type: shell
  script: validator.sh