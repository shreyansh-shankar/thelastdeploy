id: dkr-fix-broken-dockerfile
title: "Fix broken Dockerfile"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/docker-build/broken-dockerfile"
    - "echo 'FROM alpine:3.18' > $HOME/docker-build/broken-dockerfile/Dockerfile"
    - "echo 'RUNN apk add --no-cache curl' >> $HOME/docker-build/broken-dockerfile/Dockerfile"
    - "echo 'CMD [\"echo\", \"hello\"]' >> $HOME/docker-build/broken-dockerfile/Dockerfile"
    - "docker rmi -f test-fixed-df || true"
version: 1
