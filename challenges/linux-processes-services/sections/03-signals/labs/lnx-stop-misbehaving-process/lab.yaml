id: lnx-stop-misbehaving-process
title: "Terminate a misbehaving process"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "pkill -f bad_process || true"
    - "nohup bash -c 'exec -a bad_process sleep 3600' >/dev/null 2>&1 &"
